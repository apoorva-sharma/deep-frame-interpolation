%%


for i = 1:77
    
    vidframe = squeeze(frames_to_save(i,:,:,:));
    subplot(1,2,1)
    clf
    imagesc(vidframe)
    subplot(1,2,2)
    clf
    imagesc(vidframe)
    pause(1/25);
    
    vidframe = squeeze(outputs(i,:,:,:));
    baseline = 
    clf
    imagesc(vidframe/255)
    pause(1/25);
    
end